[package]
name = "pond-adapters-piper"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

# ── Build note ────────────────────────────────────────────────────────────────
# Default build is in-process `piper-rs` (ONNX Runtime + espeak-ng phonemizer).
# Subprocess fallback:
#   cargo build -p pond-adapters-piper --features legacy-subprocess
# CUDA on Jetson: piper-rs uses `ort` under the hood. ONNX Runtime CUDA EP is
# loaded dynamically at runtime when JetPack's libonnxruntime.so is on
# LD_LIBRARY_PATH. No cargo feature is wired here — the runtime EP probes the
# system. macOS uses the bundled CPU runtime; CoreML EP can be added later.

[features]
default = []
# Keep the old `Command::new(piper)` subprocess path compilable. Default builds
# omit it and drop the `synthesize_blocking` / `speak_blocking` code paths.
legacy-subprocess = []

[dependencies]
pond-core   = { workspace = true }
anyhow      = { workspace = true }
async-trait = { workspace = true }
tokio       = { workspace = true }
tracing     = { workspace = true }
# rodio is isolated here — not added to workspace deps to avoid pulling
# audio playback into every crate.
rodio = "0.19"
# cpal for microphone input monitoring (barge-in speech energy detection).
cpal = "0.15"
# In-process Piper TTS. Wraps ort (ONNX Runtime) + espeak-rs for phonemization.
# Pinned at =0.2.0: requires `ort = "=2.0.0-rc.12"`. The workspace unifies on
# rc.12 — `fastembed = "=5.16.0"` (pond-infra) and `pond-adapters-face-onnx`
# both pin the same version. The voice .onnx is found by replacing the
# `.onnx.json` config-path suffix with `.onnx`. espeak-ng-data dir is
# configured at runtime via the `PIPER_ESPEAKNG_DATA_DIRECTORY` env var,
# consumed by the transitive espeak-rs.
piper-rs = "=0.2.0"

[dev-dependencies]
pond-core = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
tempfile = { workspace = true }
