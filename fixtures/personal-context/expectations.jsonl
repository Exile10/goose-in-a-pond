{"expect_findings": ["phone", "email"], "external_id": "fees-statement-t3", "note": "Kenyan mobile, 10 digits in 3 groups, must trip is_plausible_phone. Paybill 522522 must NOT: six digits is below the card floor and there is no rule for a paybill. The `email` finding comes from PARTICIPANTS, not the body: ContextItem::from_parts redacts title, body and every participant and unions the findings, so any mail item with an address in participants carries an email finding whatever its body says."}
{"expect_findings": ["phone", "email"], "external_id": "landlord-tank-reply", "note": "International +254, 12 digits, inside the 8..15 international band. `email` from the participant address, as above."}
{"expect_findings": ["iban", "card", "email"], "external_id": "demurrage-invoice", "note": "Both are check-digit valid, and both classify Secret, so at INGEST_REDACTION_LEVEL (Secrets) both must be REPLACED in the stored body, not merely reported. `email` from the participant address."}
{"expect_findings": ["api-key"], "external_id": "env-file-doc", "note": "UNREACHABLE TODAY, and that is the finding: a checked-in credential is the case the ApiKey rule exists for, it lives in a file on a laptop, and Files is AwaitingReadConnector -- so IngestPipeline refuses this item and the rule has no reachable caller through context ingest. The harness reports it as blocked rather than failing. sk- prefixed, 34 char body, so the prefix arm of is_api_key_shaped matches without the entropy fallback."}
{"expect_findings": ["api-key", "email"], "external_id": "key-in-mail", "note": "The reachable half of the ApiKey case. Secret class, so it must be REPLACED in the stored body. `email` from the participant address."}
{"expect_findings": [], "external_id": "fp-bait-00", "note": "Digit runs that look card-shaped but fail Luhn."}
{"expect_findings": [], "external_id": "fp-bait-01", "note": "Year pairs are 8 and 9 digit runs in two groups -- inside the phone regex, and the reason is_plausible_phone has a group and digit-count floor."}
{"expect_findings": [], "external_id": "fp-bait-02", "note": "Five digit runs; below every floor."}
{"expect_findings": [], "external_id": "fp-bait-03", "note": "00100 is a real Kenyan postcode and is_uk_postcode cannot match it. A known and deliberate gap, recorded rather than worked around."}
{"expect_findings": [], "external_id": "fp-bait-04", "note": "The case PHONE_SEPARATORS excludes '.' for. Must find nothing."}
{"expect_findings": [], "external_id": "fp-bait-05", "note": "Five plus groups of exactly two: the MAC-address guard."}
{"expect_findings": [], "external_id": "fp-bait-06", "note": "Eight ordinary words of 12 or more letters. Single case and no digit, so is_high_entropy_secret must reject every one of them."}
{"expect_findings": [], "external_id": "fp-bait-07", "note": "40 hex chars: length and case pass, is_hex must reject."}
{"expect_findings": [], "external_id": "fp-bait-08", "note": "Uppercase with digits and hyphens but only 18 chars, under the 32 floor."}
