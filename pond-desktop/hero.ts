import { heroui } from "@heroui/react";
export default heroui({
  themes: {
    light: {
      colors: {
        primary: { DEFAULT: "#8C52FF", foreground: "#FFFFFF" },
        danger:  { DEFAULT: "#FF3B30", foreground: "#FFFFFF" },
        success: { DEFAULT: "#34C759", foreground: "#FFFFFF" },
      },
    },
  },
});
